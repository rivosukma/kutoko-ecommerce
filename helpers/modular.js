export const mod = {
    carousel: 'carousel',
    banner: 'banner',
    products: 'products',
    slider: 'slider'
}

// export const ModularComponent = (modular = [], components = []) => {
//     if(components.length === 0) return
//     modular
//         ?.map((mdl, index) => {
//             return(
//                 <>
//                     {mdl.component === mod.carousel && }
//                 </>
//             )
//         })
// }